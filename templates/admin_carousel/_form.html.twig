{{ form_start(form) }}
{# on rend l'itégralité du formulaire en suivant le patron (type) #}

    {# {{ form_widget(form) }} #}
    {#on rend les lignes de chaque propriétés #}

    <div class="d-flex flex-wrap justify-content-between mb-3">
        <div class="col-12 col-md-2 d-flex align-items-center">{{form_row(form.isActive)}}</div>
        <div class="col-12 col-md-4 ">{{form_row(form.rankNumber)}}</div>
        <div class="col-12 col-md-6 text-end">{{form_row(form.tag)}}</div>
    </div>
    {{form_row(form.titre)}}
    {{form_row(form.texte)}}
    {{form_row(form.imageFile)}}

    <button class="btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
