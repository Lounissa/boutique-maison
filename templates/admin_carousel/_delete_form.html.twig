<form method="post" action="{{ path('app_admin_carousel_delete', {'id': carousel.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce livre?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carousel.id) }}">
    <button class="btn btn-danger"><i class="aling-middle" data-feather="trash"></i></button>
</form>
