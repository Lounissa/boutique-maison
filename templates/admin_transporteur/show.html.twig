{% extends 'base-backend.html.twig' %}

{% block title %}Transporteur{% endblock %}

{% block body %}
<a href="{{ path('app_admin_transporteur_index') }}" class= "btn btn-dark btn-sm mb-2">Retour à la liste</a>
<div class="d-flex justify-content-between align-items-center">
    <h1>Transporteur</h1>
    {{ include('admin_transporteur/_delete_form.html.twig') }}
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ transporteur.nom }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ transporteur.contenu }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ transporteur.prix }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_admin_transporteur_edit', {'id': transporteur.id}) }}" class="btn btn-success">Modifier</a>

{% endblock %}
