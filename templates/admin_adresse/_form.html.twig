{{ form_start(form) }}
    

{{ form_row(form.adresse1) }}
{{ form_row(form.adresse2) }}
{{ form_row(form.codePostal) }}
{{ form_row(form.ville) }}
{{ form_row(form.pays) }}

<ul class="adresses"
data-index={{form.adresses|length > 0 ? form.adresses|last.vars.name + 1 : 0 }}
data-prototype="{{form_widget(form.adresses.vars.prototype)|e('html_attr') }}">
 {# on affiche les formulaires des adresses pour les adresses déjà ajouter au  users #}
 {% for adresse in form.adresses %}
 <li>{{form_row(adresse)}}</li>
 {% endfor %}
</ul>
<button type="button" class="add_item_link btn btn-success mb-3" data-collection-holder-class="adresses"> Ajouter une adresse</button>
    <button class="btn btn-success">{{ button_label|default('Sauvegarer') }}</button>
{{ form_end(form) }}
