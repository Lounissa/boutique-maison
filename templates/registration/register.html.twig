{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<br></br>

    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

     <p class="h3 mb-3 font-weight-normal fw-bold"> NOUVEAUX CLIENTS</p>
    <h1 class="h3 mb-3 font-weight-normal"> S'INSCRIRE MAINTENANT</h1>
    <p> Pour des raisons de sécurité, si vous n'avez pas acheté chez nous depuis 12 mois, veuillez suivre nos étapes simples et rapides pour vous réinscrire maintentant.</p>

    <br></br>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-success mt-3">S'INSCRIRE</button>
    {{ form_end(registrationForm) }}
{% endblock %}
